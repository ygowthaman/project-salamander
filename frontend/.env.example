# REST base URL for the backend
VITE_API_URL=http://localhost:8000

# WebSocket base URL for the backend.
# Set this explicitly — the in-code fallback is a stale LAN IP.
VITE_WS_URL=ws://localhost:8000
